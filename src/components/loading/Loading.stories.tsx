import React from 'react';
import Loading from '.';

export default {
    title: 'Components /  Loading',
    parameters: {
        component: Loading,
    },
};

export const loading = () => (
    <Loading />
);
